"""
什么是通信协议：
    协议即规则，就好比汽车上路要遵守交通规则一样，为了使全世界不同类型的计算机
都可以连接起来，所以制定了一套全球通用的通信协议-Internet协议。有了Internet
协议，任何私有网络，只要支持这个协议，就可以接入互联网。
TCP/IP协议
    IP协议是整个TCP/IP协议族的核心
    IP地址就是互联网上计算机的唯一标识
    目前的IP地址有两种表示方式，即IPv4和IPv6
    在命令行下使用ipconfig命令可以查看本机的IP地址
TCP/IP协议：
    TCP（Transmission Control Protocol）协议即传输控制协议，是建立在IP
协议基础之上的。TCP协议负责两台计算机之间建立可靠连接，保证数据包按顺序发送到。
它是一种可靠的、一对一的、面相有连接的通信协议。
端口号：
    区分计算机中的运行的应用程序的整数
    端口号的取值范围是0到65535，一共65536个，其中80这个端口号分配给了HTTP服务，
    21这个端口号分配给了FTP服务，3306分给了MySQL数据库，。
"""